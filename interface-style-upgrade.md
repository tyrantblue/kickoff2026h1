# 界面风格升级方案

## 当前问题

现在页面的完成度是够的，但视觉语言偏常见：蓝白底色、卡片网格、圆角容器、指标卡、常规导航。这种结构清晰，但很容易像通用 SaaS 看板或模板站，汇报时缺少个人辨识度和记忆点。

优化方向不建议只换一套颜色，而是重新确定页面气质：它不是一个普通 dashboard，而是一次面向业务结果的个人工作汇报。因此页面应该有“汇报现场感”“业务推进感”和“一眼能记住的视觉符号”。

## 设计目标

1. 保持内容可读，不能为了炫技牺牲汇报效率。
2. 让页面不像模板，形成一个明确的主题风格。
3. 支持边讲边展示，滚动、导航、重点信息都要有舞台感。
4. 样式更新要集中在主题变量和少量组件上，后续方便继续迭代。

## 推荐方向：工作流航线图

核心概念：把 H1 汇报设计成一条从“交付效率”到“业务结果”再到“H2 规划”的航线。

视觉关键词：航线、坐标、节点、推进、复盘、下一站。

页面不再是普通卡片堆叠，而是像一份轻量的作战地图 / 项目航线图：

- 首页使用 OneAIX logo 作为起点标识，主标题旁出现简洁的坐标线或路线编号。
- 每个 section 是一段航线节点，不是普通白卡片。
- OKR 卡片可以变成“任务节点”，O1/O2/O3 是主节点，KR 是节点内的检查项。
- 图表区域可以做成“仪表盘带”，但减少传统柱状图的模板感，强调进度轨迹。
- 导航栏可以改为路线导航，当前 section 像“当前站点”被点亮。

这个方向和你的汇报内容最贴合，因为你的主题本身就是交付、质量、AI 工具、Playwright、业务支撑。它会显得专业，但不会像普通企业模板。

## 色彩方案

避免继续使用普通蓝白作为主视觉。当前采用 `color.amfe.space` 风格里的“冬日蓝”思路，主色收敛到偏科研图表的低饱和蓝灰：

- 页面基底：纸感浅灰 `#F6F4F0`
- 中性面：温和灰米色 `#D2D1C4`
- 主强调：冬日蓝 `#1A6A96`
- 次强调：蓝灰 `#7C9CA8`
- 深色支撑：深海蓝灰 `#1F5360`
- 深色模式：深蓝黑 `#101C39` 到 `#1F5360`
- 风险 / 未完成：保留少量赤陶红 `#D9543F`

这样能避开常见的蓝紫渐变、深蓝大屏、浅蓝 dashboard，同时比高饱和产品色更稳。

## 科研配色参考

可以从 `https://color.amfe.space/` 这类科研配色站点找灵感。它的价值不是直接复制某一组颜色，而是借鉴论文图表里的几个原则：

1. 颜色数量克制，通常 4 到 6 个主色足够。
2. 色相之间有明显区分，但饱和度不会过高。
3. 强调色承担信息层级，而不是把页面染成单一品牌色。
4. 暗色、暖色、冷色同时存在，页面会更有厚度。
5. 完成、部分完成、未完成这些状态色要能和主视觉共存，不要像默认 UI 库颜色。

当前页面采用“学术图表 + 航线图”的混合配色：

- 基底色：象牙纸色 / 石墨黑，用来建立报告质感。
- 主线色：低饱和冬日蓝，用来表示路线、完成、当前节点。
- 辅助色：蓝灰与灰米色，用来强调关键结论和首页价值观。
- 对比色：赤陶红，用来表示未完成或风险。
- 可扩展色：深海蓝灰，只在图表系列需要更多区分时使用。

这类配色比常见 SaaS 蓝、紫色渐变、纯黑大屏更耐看，也更适合汇报场景。

## 布局改造

### 1. 首页

从普通 hero 改成“汇报封面 + 核心坐标”：

- 左侧保留标题、周期、价值观文案。
- 右侧不再是普通指标卡，而是一个“核心结果坐标面板”。
- 四个指标以坐标点或路线节点展示：工单、时长、模块、测试通过率。
- 背景可加入细线网格、坐标刻度、轻微扫描线，但不能太重。

### 2. 内容区

每个 section 使用统一的“站点面板”：

- section 顶部有站点编号，例如 `NODE 01 / OKR REVIEW`。
- 左侧固定为标题和概述，右侧为内容主体。
- 桌面端形成左右结构，移动端再折叠成上下结构。
- 这样比全宽大卡片更像汇报系统，也更有设计感。

### 3. OKR 卡片

把普通卡片改成“任务板块”：

- O 编号做成明显的垂直编号，例如 `O1` 占据卡片左上角。
- KR 不再像普通小卡片，而像 checklist / ticket。
- 已完成、部分完成、未完成使用不同左边框或状态点，不只依赖 badge。
- 对齐继续保留，避免视觉整齐度回退。

### 4. KISS

KISS 可以改成四象限复盘板，但不要做常见彩色卡片。

- Keep / Improve / Start / Stop 用四个方向标签。
- 每项像便签或 review item，但整体保持克制。
- Stop 可以用较浅的退场样式，Start 可以用更亮的强调线。

### 5. 业务结果

业务结果适合改成“证据链”：

- 左侧展示一句业务结论。
- 右侧按证据卡排列：711 工单、27m、95%+、245 模块、90%+。
- 每张证据卡都回答一个问题：它对业务有什么帮助。

### 6. H2 + Q3

规划部分适合做“下一阶段路线图”：

- 三个 O 横向排列成路线的三段。
- KR 作为每段路线下的里程碑。
- 攻坚事项可以接在路线图后，作为重点关卡。

## 动效建议

现在已有 section 聚焦动效，后续可以改成更有主题感的动效：

- 页面滚动时，当前节点的路线线条被点亮。
- 导航切换不做大幅模糊，改成节点高亮和轻微位移。
- 卡片进入时使用短距离 slide + opacity，不要弹性过强。
- 图表数值可以在进入视口时计数增长，但只触发一次。
- 背景网格或路线线条可以有极轻微移动，增强现场感。

动效原则：汇报场景里，动效是引导视线，不是抢话。

## 技术实现建议

继续使用当前技术栈：

- React 负责组件结构。
- Tailwind 负责大部分布局和状态样式。
- Framer Motion 负责 section、卡片、路线节点动画。
- Recharts 可以保留，但图表样式要定制，减少默认图表味。

建议新增这些文件：

- `src/constants/theme.js`：集中管理颜色、状态、路线节点等设计 token。
- `src/components/RouteFrame.jsx`：统一 section 的航线框架。
- `src/components/RouteNode.jsx`：统一节点编号、状态点、连线。
- `src/components/KrTicket.jsx`：统一 KR 项样式。
- `src/components/EvidenceCard.jsx`：业务证据卡。

## 迭代顺序

1. 先改全局视觉变量：背景、文字、强调色、边框、阴影。
2. 再改 section 框架：从普通大卡片变成站点面板。
3. 改首页：建立“航线图”第一印象。
4. 改 OKR 和 OKR 规划：把两块统一成任务节点风格。
5. 改业务结果：从普通卡片变成证据链。
6. 最后调动效：路线高亮、进入动画、数字动画。

## 我推荐的第一版落地范围

第一轮不要一次性重写所有组件。建议先做：

1. 全局色彩和背景网格。
2. 首页重构。
3. section 标题改成节点式标题。
4. OKR / OKR 规划两块改成任务节点风格。

这四项完成后，页面气质会明显从“模板看板”变成“汇报路线图”，而且不会影响内容维护。
